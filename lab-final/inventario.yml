---
# Inventário - Projeto de Backup e Auditoria - Filial

all:
  children:
    switches:
      hosts:
        SW1:
          ansible_host: 192.168.50.11
    routers:
      hosts:
        R1:
          ansible_host: 192.168.50.1

  vars:
    ansible_user: "{{ vault_user }}"
    ansible_password: "{{ vault_password }}"
    ansible_connection: network_cli
